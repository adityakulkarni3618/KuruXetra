# KURUXETRA SPORTS MANAGEMENT SYSTEM (KSMS) - PROJECT DIGEST
===========================================================

## Current Status
- Authentication transitioned fully to Athletic ID (`uniqueId`) with password confirmation; `username` concept dropped.
- Completed feature additions for admin/captain workflows, workout logging, running logging, profile editing, and multiple sports secretaries.
- New profile page added to dashboard and backend profile picture/college ID update endpoints implemented.
- Admin `/api/admin/users/search` and `/api/admin/users/:id/promote-to-ss` are active and support multiple super admins.
- Seed data defaults to Admin ID `KX000001` and password `SSSS@123`.
- Backend and frontend builds both pass.

## Project Stack
- **Backend**: Node.js, Express, TypeScript, PostgreSQL (Neon DB Cloud), Prisma, JWT authentication.
- **Frontend**: Next.js 15 (App Router), TypeScript, Tailwind CSS, Recharts.

---

## Directory Structure
- `/backend`
  - `src/index.ts`: Express server entrypoint, API routing, CORS, JSON body parsing, health check.
  - `src/routes/`: API router modules for auth, sports, attendance, workouts, running, leaderboard, users, admin.
  - `prisma/`
    - `schema.prisma`: Core database schema.
    - `seed.ts`: Seed script for Super Admin and demo sports.
  - `package.json`: Backend scripts (`dev`, `build`, `start`, `prisma:generate`, `prisma:migrate`, `prisma:seed`).
- `/frontend`
  - `app/`: Next.js App Router with root, login, register, dashboard, and nested dashboard pages.
  - `app/dashboard/`: Protected dashboard area with admin, attendance, captain, leaderboard, running, sports, and workouts views.
  - `components/`: Shared UI components.
  - `lib/`: Shared frontend helpers including `api.ts` and `auth-context.tsx`.
  - `package.json`: Frontend scripts (`dev`, `build`, `start`, `lint`).

---

## Backend Details
- Uses `tsx watch src/index.ts` for local development.
- Backend listens on port `4000` by default and exposes `/health`.
- CORS origin uses `process.env.CORS_ORIGIN` or defaults to `http://localhost:3000`.
- Primary API routes:
  - `POST /api/auth/*`
  - `GET/POST /api/sports/*`
  - `GET/POST /api/attendance/*`
  - `GET/POST /api/workouts/*`
  - `GET/POST /api/running/*`
  - `GET /api/leaderboard/*`
  - `GET/POST /api/users/*`
  - `GET/POST /api/admin/*`

---

## Frontend Pages
- `app/page.tsx`: Root landing page.
- `app/login/page.tsx`: Login screen.
- `app/register/page.tsx`: Registration screen.
- `app/dashboard/layout.tsx`: Dashboard layout wrapper.
- `app/dashboard/page.tsx`: Dashboard home.
- `app/dashboard/admin/page.tsx`: Admin dashboard.
- `app/dashboard/attendance/page.tsx`: Attendance view.
- `app/dashboard/captain/page.tsx`: Captain area.
- `app/dashboard/leaderboard/page.tsx`: Leaderboard.
- `app/dashboard/running/page.tsx`: Running log view.
- `app/dashboard/sports/page.tsx`: Sports management view.
- `app/dashboard/workouts/page.tsx`: Workout log view.

---

## Core Database Schema (Prisma)
- **Role Enum**: `SUPER_ADMIN`, `CAPTAIN`, `STUDENT_ATHLETE`, `FITNESS_MEMBER`
- **UserStatus Enum**: `PENDING_APPROVAL`, `ACTIVE`, `SUSPENDED`
- **MembershipStatus Enum**: `PENDING`, `APPROVED`, `REJECTED`, `REMOVED`
- **Models**:
  - `Season`: Tracks sports seasons and optionally links memberships, attendance, workouts, runs, and points.
  - `User`: Stores user identity, academic details, contact info, role, status, and related activity records.
  - `Sport`: Stores sport metadata, captain relation, practice info, capacity, and status.
  - `Membership`: Joins a user to a sport with approval status and optional season.
  - `Attendance`: Stores check-in/out, duration, sport, season, and marking source.
  - `Workout`: Records exercise entries, sets, reps, weight, duration, calories, heart rate, notes, and optional photo URL.
  - `RunningLog`: Records distance, duration, pace, speed, calories, and notes.
  - `PointsLedger`: Append-only points ledger with reason and optional reference ID.

---

## Security Practices & Architecture
1. **Secret & Key Isolation**: Sensitive parameters (`DATABASE_URL`, `JWT_SECRET`, and Cloudinary upload credentials) are completely externalized from the source code and loaded dynamically through environment variables (`.env`).
2. **Data Sanitization**: Direct ORM queries using Prisma prevent SQL injection attacks. Inputs are strictly validated on REST API entry points using schemas.
3. **Role-Based Access Control (RBAC)**: All administrative, captain review, and user management routes are protected by backend route guards validation logic.
4. **Password Security**: Passwords are securely hashed with bcrypt (10 rounds salting strength) before storage. Unencrypted credentials are never logged or exposed.
5. **Session Management**: Session tokens are encrypted using JSON Web Tokens (JWT) with configured expiration periods.

---

## Public Repository Guidelines
> [!IMPORTANT]
> Maintaining the GitHub repository as public is **completely safe** provided that these rules are strictly enforced:
> - **Environment Variables (.env)**: Never commit the `.env` file or raw passwords (e.g. Neon connection strings) to GitHub. Keep `.env` in the `.gitignore` list.
> - **Build/Key Secrets**: Manage API passwords, Cloudinary credentials, and JWT hashes exclusively through Vercel and Render Environment Variables settings pages.
> - **Default Admin Credentials**: Change the seeded default admin password (`SSSS@123`) inside the production database immediately.
